{
  "name": "EventBookingSystem",
  "private": true,
  "type": "module",
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*"
    ],
    "catalog": {
      "dotenv": "^17.2.2",
      "zod": "^4.1.11",
      "tsdown": "^0.15.5"
    }
  },
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "build": "bun run --filter '*' build",
    "check-types": "bun run --filter '*' check-types",
    "dev:native": "bun run --filter native dev",
    "dev:web": "bun run --filter web dev",
    "dev:server": "bun run --filter server dev",
    "db:push": "bun run --filter @EventBookingSystem/db db:push",
    "db:studio": "bun run --filter @EventBookingSystem/db db:studio",
    "db:generate": "bun run --filter @EventBookingSystem/db db:generate",
    "db:migrate": "bun run --filter @EventBookingSystem/db db:migrate"
  },
  "packageManager": "bun@1.2.19",
  "dependencies": {
    "dotenv": "^17.2.2",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "tsdown": "^0.15.5",
    "@types/bun": "^1.2.6"
  }
}
